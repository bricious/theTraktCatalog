# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'The Trakt Catalog' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 4.0'
  pod 'SDWebImage', '~>3.8'
  pod 'ModelMapper'
  # Pods for The Trakt Catalog

  target 'The Trakt CatalogTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'The Trakt CatalogUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
